# Instructions for Claude

This project uses plain `.jsx` (not `.tsx` — there's no TypeScript here).

Whenever you edit any file under `src/` that affects rendered UI (components, `App.jsx`, `App.css`), run the `update-screenshots` skill to regenerate the README screenshots in `docs/`.
